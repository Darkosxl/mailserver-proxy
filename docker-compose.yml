services:
  proxy:
    build: .
    container_name: mailserver-proxy
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - TARGET_URL=http://host.docker.internal:8080
      - PORT=3000
